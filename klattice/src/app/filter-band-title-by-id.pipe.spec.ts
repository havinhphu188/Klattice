import { FilterBandTitleByIdPipe } from './filter-band-title-by-id.pipe';

describe('FilterBandTitleByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBandTitleByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
