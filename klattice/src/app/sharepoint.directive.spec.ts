import { SharepointDirective } from './sharepoint.directive';

describe('SharepointDirective', () => {
  it('should create an instance', () => {
    const directive = new SharepointDirective();
    expect(directive).toBeTruthy();
  });
});
