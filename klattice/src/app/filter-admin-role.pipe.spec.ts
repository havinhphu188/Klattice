import { FilterAdminRolePipe } from './filter-admin-role.pipe';

describe('FilterAdminRolePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdminRolePipe();
    expect(pipe).toBeTruthy();
  });
});
