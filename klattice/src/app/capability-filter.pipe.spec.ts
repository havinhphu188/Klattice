import { CapabilityFilterPipe } from './capability-filter.pipe';

describe('CapabilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapabilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
