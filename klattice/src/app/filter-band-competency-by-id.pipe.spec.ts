import { FilterBandCompetencyByIdPipe } from './filter-band-competency-by-id.pipe';

describe('FilterBandCompetencyByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBandCompetencyByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
