import { FilterTitleByIdPipe } from './filter-title-by-id.pipe';

describe('FilterTitleByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTitleByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
